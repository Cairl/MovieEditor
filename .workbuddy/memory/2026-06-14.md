# 2026-06-14

## 代码审查 + 修复：同事的 8 项修改

审查了同事对 MovieEditor 的 8 项修改（handler_name、运行标题、命令预览、视频轨 -map、日志格式、Map 顺序、硬件编码、Ctrl+C 修复）。

### 发现并修复的问题

1. **ffmpeg.py L389**: `_shutdown_requested` 分支 `except OSError` → `except (OSError, subprocess.TimeoutExpired)`。`TimeoutExpired` 不是 `OSError` 子类，极端情况下会逃逸。
2. **logger.py L143**: `exc_info=error` → `exc_info=(type(error), error, error.__traceback__)`。Python logging 的 `exc_info` 不接受 Exception 对象。
3. **app.py L393**: 批处理 `except (OSError, RuntimeError)` 中添加 `log_ffmpeg_error('batch', e)`。该函数被定义/导入但从未调用。
4. **app.py L148-150**: QSV 加 `-global_quality 23`，AMF 加 `-quality balanced`。之前缺质量参数。
5. **ffmpeg.py/ app.py**: 清理 `format_preview_lines` 的死参数 `input_file`/`output_file`，移除 `Optional` import。

## 输出路径修改

改 `build_ffmpeg_command` 输出路径逻辑：
- 剧集：`<parent> (MovieEditor+YYYYMMDD-HHMMSS)/<stem>.mp4`
- 电影：`<stem> (MovieEditor+YYYYMMDD-HHMMSS).mp4`
- `batch_timestamp` 在 `process_files()` 开头生成一次，传入所有 3 个调用点
- `os.makedirs` 移到 `build_ffmpeg_command` 内部，调用端不再单独创建目录

## 视频设置菜单修复

1. **H.265 提示动态化**: 原来写死 `-c:v hevc -crf 23`，现在根据当前硬件编码器动态生成（nvenc→hevc_nvenc, qsv→hevc_qsv, amf→hevc_amf, CPU→libx265）
2. **硬件编码器右侧提示**: 用 `with_ffmpeg_hint` 包装硬件编码行，右侧参数栏显示对应 FFmpeg 参数
3. **返回菜单样式**: 视频/音频/字幕三个设置子菜单的返回行统一加上 `←` 后缀 + muted 颜色

### LEFT/RIGHT 排查 → 找到真因

追查 `run_menu_loop` → `no_nav_indices` → `action_handler` 链路无果后，发现根因在 `console.py:_check_console_ctrl()`：
- 旧代码用 `ReadConsoleInputW` 消费任意键盘事件，非 Ctrl+C 也被吞掉
- 箭头键在 `ReadConsoleInputW` 被消耗后，`msvcrt.getch()` 的 `kbhit()` 返回 False，键永远丢失
- 修复：`PeekConsoleInputW` 非破坏性检测 → 仅在确认 vk==0x43 且 Ctrl 按下时才 `ReadConsoleInputW` 消费
- 同时修复了 console.py 中两处 UTF-8 损坏字符（L85 分隔符检测、L96 focus 图标）

## Shift+方向键分钟调整 + 界面微调

1. **navigation.py**: LEFT/RIGHT 增加 Shift 检测 → 返回 'SHIFT_LEFT'/'SHIFT_RIGHT'
2. **display.py**: key filter 加入 SHIFT_LEFT/SHIFT_RIGHT
3. **video.py action_handler**: 时间调节支持 SHIFT_LEFT/SHIFT_RIGHT → delta=60 秒
4. **video.py hint**: `-c:v libx265` → `-c:v hevc` (通用名)
5. **返回图标**: `←` → `«` (三个设置文件统一)

## 修复输出目录嵌套 Bug

**问题**: `ui/app.py:110` 剧集模式输出目录嵌套在输入目录内部。
- 旧: `os.path.join(parent_dir, f'{parent_name} (MovieEditor+{timestamp})')` → `D:\TV\Season1\Season1 (MovieEditor+ts)\`
- 新: `os.path.join(os.path.dirname(parent_dir), f'{parent_name} (MovieEditor+{timestamp})')` → `D:\TV\Season1 (MovieEditor+ts)\`
- 电影模式不受影响（series_mode=False 分支未改动）

## 修复多菜单渲染重叠 Bug

**问题**: 逐集模式处理后旧菜单未清除，视频设置+字幕设置+FFmpeg进度UI同时可见。

**根因**: `core/ffmpeg.py` draw_full_interface 使用 `\033[H\033[J` 清屏（某些Windows终端不可靠），且未重置 LAST_MENU_LINES 增量渲染缓存。

**修复** (3处):
1. L324: `\033[H\033[J` → `\033[2J\033[H`（与 render_menu_box 一致）
2. L325: 新增 `reset_menu_cache()` 调用
3. L374: shimmer `print(...)` 末尾加 `\033[K`
4. L16: 新增 `from ui.display import reset_menu_cache`

**问题**: `ui/app.py:110` 剧集模式输出目录嵌套在输入目录内部。
- 旧: `os.path.join(parent_dir, f'{parent_name} (MovieEditor+{timestamp})')` → `D:\TV\Season1\Season1 (MovieEditor+ts)\`
- 新: `os.path.join(os.path.dirname(parent_dir), f'{parent_name} (MovieEditor+{timestamp})')` → `D:\TV\Season1 (MovieEditor+ts)\`
- 电影模式不受影响（series_mode=False 分支未改动）
