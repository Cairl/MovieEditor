### 添加

- **命令换行续行缩进**: 长路径换行后自动对齐参数值起始位置，含引号路径跳过引号对齐
- **ffmpeg 框高度限制**: 进度框高度上限对齐设置菜单，超出时压缩命令行显示区域
- **返回菜单右对齐**: 返回菜单行右对齐到 `MENU_LABEL_WIDTH`，光标 `›` 紧贴标签前方而非行首

### 修复

- **标题边框宽度不足**: 单列菜单标题 `╭──── 标题 ────╮` 右侧缺 2 格（简化器引入）
- **上下文行右 padding 缺失**: 菜单上下文行右侧少 2 格空白导致边框不对齐
- **选中行字体不提亮**: 含灰色（muted）样式的行选中后字体仍为暗色，现在统一提亮
- **含空格路径未加引号**: ffmpeg 命令中含空格的输出路径在显示和复制时均未加引号
- **DOWN 滚动无法到底**: 命令行 DOWN 键滚动上限使用未换行行数，换行后无法滚到底部
- **返回菜单溢出边框**: 右对齐项未选中时多算 2 格导致文字溢出右边框

### 优化

- **全局 UI 缩进标准**: 菜单内容左边距 2 格、分隔线距边框 1 格、标题 `╭───` 统一 3 格
- **ffmpeg 界面独立缩进**: 进度文字 4 格、进度条 2 格、按钮 4 格，命令起点从 4 格改为 2 格
- **交互按钮选中高亮**: 灰色背景左右各延伸一格到间隔空间，保留按钮原色字体
- **返回菜单样式**: 统一使用 `menu_return_item`，去掉 `«` 符号，下方保留一行间距
- **代码简化**: 提取 `_display_width`/`_char_width`/`_sanitize_title` 辅助函数，缓存命令换行结果，合并重复扫描
