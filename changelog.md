### 添加

- **硬件编码选项**: 视频设置新增硬件编码器选择，支持 CPU (默认)、NVIDIA NVENC、Intel QSV、AMD AMF 四种模式。QSV 和 AMF 编码器已添加质量控制参数

### 修复

- **视频轨缺失**: 指定 `-map` 音频或字幕流时正确映射视频流，修复渲染输出只有音轨无画面的问题
- **Ctrl+C 进程残留**: 修正 Ctrl+C 打断后 FFmpeg 子进程未终止的遗留问题
- **handler_name 标记**: 视频元数据 handler_name 改为 @Cairl
- **硬件编码参数预览**: 修正 H.265 编码的 FFmpeg 参数提示始终显示 CPU 编码的问题。H.265 行和硬件编码行参数拆分显示（编码器 vs 编码参数），消除重复提示
- **左右方向键被吃掉**: 修复 `_check_console_ctrl` 使用 `ReadConsoleInputW` 消耗非 Ctrl+C 事件导致 LEFT/RIGHT 方向键偶发失效的问题。改为 `PeekConsoleInputW` 非破坏性检测，仅在确认是 Ctrl+C 时才消费
- **视频时间调节**: 开始/结束时间初始状态按下右键从 00:00:00 开始递增，步长改为 1 秒，Shift+←→ 按分钟步进，支持长按连续滚动

### 优化

- **运行界面标题**: 显示差异文件名（剧集信息），替代冗长的完整文件路径
- **运行界面命令预览**: 预览展示真实输入输出路径，替代 `<input>`/`<output>` 占位符
- **Map 参数顺序**: 统一为视频→音频→字幕
- **日志格式标准化**: 时间戳启用 ISO 8601 格式（含毫秒），日志级别对齐为 5 字符宽度，错误日志包含完整堆栈跟踪
- **输出路径**: 输出文件/目录更名为 `(MovieEditor+时间戳)` 格式，剧集输出到输入目录旁的同名时间戳目录，电影输出到输入旁的同名时间戳文件
- **返回菜单样式**: 三个设置子菜单的返回行增加 « 引导符号，使用 muted 灰色与普通设置行区分
